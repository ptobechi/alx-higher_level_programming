-- Script to insert a new row in the table first_table from the
-- database hbtn_0c_0
-- Checking if the argument (database name) is provided */
USE hbtn_0c_0;

-- Query to insert a new row into the table first_table */
INSERT INTO first_table (id, name) VALUES (89, 'Best School');
